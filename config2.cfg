// Конфигурация базы данных
const db_host = "localhost"
const db_port = 5432

struct {
    db_name = "my_database",
    user = "db_user",
    password = "secret",
    host = $[db_host],
    port = $[db_port],
}
